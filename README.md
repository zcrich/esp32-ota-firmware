# esp32-ota-firmware

Public firmware release repo for [esp32-ota](https://github.com/zcrich/esp32-ota). Contains only release artifacts (`latest.json` manifest and `firmware-vX.Y.Z.bin` binaries) — no source code, no secrets.
